<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="TextStyle">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textStyle">bold</item>
    </style>
    
    <style name="HeaderTextStyle" parent="TextStyle">
        <item name="android:textSize">@dimen/header_text_size</item>
        <item name="android:background">@color/header_background</item>
    </style>

    <style name="ItemTextStyle" parent="TextStyle">
        <item name="android:textSize">@dimen/item_text_size</item>
    </style>

    <style name="SubItemTextStyle" parent="TextStyle">
        <item name="android:textSize">@dimen/subitem_text_size</item>
    </style>

</resources>
